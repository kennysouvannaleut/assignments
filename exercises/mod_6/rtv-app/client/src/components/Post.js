import React from 'react';

const Post = props => {
    const { date, title, description, votes } = props;

    return (
        <div className='post'>     

        </div>
    );
};

export default Post;